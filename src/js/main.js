//mainJs 

const clickOne = () => {
  store.dispatch({ type: 'conversionClick', id: 0 });
};

const clickTwo = () => {
  store.dispatch({ type: 'conversionClick', id: 1 });
};

const clickThree = () => {
  store.dispatch({ type: 'conversionClick', id: 2 });
};

